ServiceMaps:
  serviceName: Health Notification Service
  version: 1.0.0
  mappings:
    # ── INDEX: Save new scheduled notification to Elasticsearch ──
    - topic: nigeria-itn-save-scheduled-notification-topic
      configKey: INDEX
      indexes:
        - name: nigeria-itn-scheduled-notification-v1
          type: general
          id: $.id
          isBulk: true
          jsonPath: $.*
          timeStampField: $.auditDetails.createdTime

    # ── INDEX: Update scheduled notification in Elasticsearch ──
    - topic: nigeria-itn-update-scheduled-notification-topic
      configKey: INDEX
      indexes:
        - name: nigeria-itn-scheduled-notification-v1
          type: general
          id: $.id
          isBulk: true
          jsonPath: $.*
          timeStampField: $.auditDetails.lastModifiedTime

    # ── REINDEX: Re-index from Elasticsearch ──
    - topic: nigeria-itn-scheduled-notification-v1
      configKey: REINDEX
      indexes:
        - name: nigeria-itn-scheduled-notification-v1
          type: general
          jsonPath: $.hits
          timeStampField: $.lastModifiedTime
ServiceMaps:
  serviceName: plan-service
  version: 1.0.0
  mappings:
    - topic: kw-save-plan
      configKey: INDEX
      indexes:
        - name: kw-plan-index-v1
          type: general
          id: $.id
          timeStampField: $.auditDetails.createdTime
          jsonPath: $.Plan
          customJsonMapping:
            indexMapping: {"Data":{}}
            fieldMapping:
              - inJsonPath: $
                outJsonPath: $.Data

    - topic: kw-update-plan
      configKey: INDEX
      indexes:
        - name: kw-plan-index-v1
          type: general
          id: $.id
          timeStampField: $.auditDetails.createdTime
          jsonPath: $.Plan
          customJsonMapping:
            indexMapping: { "Data": { } }
            fieldMapping:
              - inJsonPath: $
                outJsonPath: $.Data

    - topic: kw-bulk-update-plan
      configKey: INDEX
      indexes:
        - name: kw-plan-index-v1
          type: general
          id: $.id
          isBulk: true
          timeStampField: $.auditDetails.createdTime
          jsonPath: $.Plans
          customJsonMapping:
            indexMapping: { "Data": { } }
            fieldMapping:
              - inJsonPath: $
                outJsonPath: $.Data

    - topic: kw-save-plan-facility
      configKey: INDEX
      indexes:
        - name: kw-plan-facility-index-v1
          type: general
          id: $.id
          timeStampField: $.auditDetails.createdTime
          jsonPath: $.PlanFacility
          customJsonMapping:
            indexMapping: { "Data": { } }
            fieldMapping:
              - inJsonPath: $
                outJsonPath: $.Data

    - topic: kw-update-plan-facility
      configKey: INDEX
      indexes:
        - name: kw-plan-facility-index-v1
          type: general
          id: $.id
          timeStampField: $.auditDetails.createdTime
          jsonPath: $.PlanFacility
          customJsonMapping:
            indexMapping: { "Data": { } }
            fieldMapping:
              - inJsonPath: $
                outJsonPath: $.Data
